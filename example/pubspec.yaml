name: example
description: A new Flutter application.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  calendar_event:
    path: ../../calendar_event

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
